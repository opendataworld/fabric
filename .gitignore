# Proof-of-use validator — kept on disk, intentionally not committed
tools/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Env / secrets
.env
*.local

# Runtime data (signups contain PII — never commit)
api/data/

# Rust build output
fabric-db/target/
