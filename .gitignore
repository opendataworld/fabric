# tools/ is local-only (proof-of-use validator), EXCEPT the platform CLI
tools/*
!tools/fabric.py

# Python
__pycache__/
*.pyc
.pytest_cache/

# Env / secrets
.env
*.local

# Runtime data (signups contain PII — never commit)
api/data/

# Rust build output
fabric-db/target/

# Node / TypeScript
web/node_modules/
web/dist/

# Nextcloud-on-k3s: never commit real secrets (only the *.example.yaml)
deploy/k3s/nextcloud/01-secrets.yaml
deploy/k3s/nextcloud/99-cert-manager.yaml
deploy/k3s/registry/02-htpasswd.yaml
